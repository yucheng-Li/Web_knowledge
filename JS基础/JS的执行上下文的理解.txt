每当一个函数被调用时都会创建一个函数执行上下文；执行上下文又称为调用栈


当调用一个函数时，就会为该函数创建一个执行上下文，一个执行上下文的生命周期可以分为两个阶段：
1.创建阶段在这个阶段中，执行上下文会分别创建变量对象，建立作用域链，以及确定this的指向。
2.代码执行阶段创建完成之后，就会开始执行代码，这个时候，会完成变量赋值，函数引用，以及执行其他代码。
~可见，作用域链是在执行上下文的创建阶段建立的。

